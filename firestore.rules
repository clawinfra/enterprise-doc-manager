rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV ONLY: open access — lock down in Phase 7 before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
